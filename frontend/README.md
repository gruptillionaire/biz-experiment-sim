# Frontend

React and TypeScript interface for the Business Experiment Simulator.

```powershell
npm install
npm run dev
```

The app expects the API at `http://127.0.0.1:8000`. Copy `.env.example` to `.env` and change `VITE_API_URL` when using another address.

Quality checks:

```powershell
npm run lint
npm run build
```
